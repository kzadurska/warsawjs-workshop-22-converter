const examples = {
  add: (a, b) => {
    return a + b;
  },
  hello: (name) => {
    return `Hello, ${name}`
  },
  fibonacci: (number) => {
    const fibonacci = 0
    const array = [1, 1]
    if (number > 2) {
      for (let i = 1; i < number + 1; i++) {

      }
    }

    // return array[number]
    return number
  }
}

export default examples